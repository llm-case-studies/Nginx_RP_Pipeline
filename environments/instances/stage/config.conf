#!/usr/bin/env bash
# Stage environment - IONOS staging deployment

# Load base Type-5 configuration
source "${CONFIG_ROOT}/base/type-5-deploy.conf"

# Stage-specific overrides
DEPLOYMENT_ROOT="/home/deploy/pre-prod-stage/proxyuser"
ENVIRONMENT="stage"
DEPLOYMENT_SSH="deploy@stage"