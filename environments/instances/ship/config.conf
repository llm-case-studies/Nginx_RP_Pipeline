#!/usr/bin/env bash
# Ship environment - Local final verification

# Load base Type-5 configuration  
source "${CONFIG_ROOT}/base/type-5-deploy.conf"

# Ship-specific overrides
DEPLOYMENT_ROOT="$WORKSPACE_ROOT/workspace/ship"
ENVIRONMENT="ship"

# Ship uses existing production network for service connectivity
# This allows ship to connect to vaultwarden-ship and other services
LEGACY_NETWORK="pronunco-production"  # Temporary until all services use deterministic naming