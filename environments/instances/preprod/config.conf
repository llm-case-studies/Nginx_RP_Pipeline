#!/usr/bin/env bash
# Pre-prod environment - Pre-production verification

# Load base Type-5 configuration
source "${CONFIG_ROOT}/base/type-5-deploy.conf"

# Pre-prod specific overrides
DEPLOYMENT_ROOT="/home/proxyuser/pre-prod"
ENVIRONMENT="preprod"
DEPLOYMENT_SSH="proxyuser@preprod"