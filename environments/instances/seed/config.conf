#!/usr/bin/env bash
# Seed environment - Production replica for local development

# Load base Type-5 configuration  
source "${CONFIG_ROOT}/base/type-5-deploy.conf"

# Seed-specific overrides
DEPLOYMENT_ROOT="${WORKSPACE_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/workspace/seed"
ENVIRONMENT="seed"