#!/usr/bin/env bash
# WIP environment - Development workspace

# Load base Type-5 configuration  
source "${CONFIG_ROOT}/base/type-5-deploy.conf"

# WIP-specific overrides
DEPLOYMENT_ROOT="${WORKSPACE_ROOT:-$(git rev-parse --show-toplevel 2>/dev/null || pwd)}/workspace/wip"
ENVIRONMENT="wip"